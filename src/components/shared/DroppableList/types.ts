export interface ListItem {
  id: string;
  label: string;
  type?: string;
}

export interface SortOptions {
  sortOptions: ListItem[];
}
/* export interface SortOptions  {
  sortOptions: {

    id: string,
    type: string,
  label: string
}
  }
 */
