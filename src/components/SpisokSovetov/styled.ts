import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    width: "100%",
    height: "100%",
    marginTop: 15,
    marginBottom: 40
  },
  title: {
    textAlign: "center",
    fontWeight: 500,
    fontSize: 20,
    marginBottom: 15,
  },
  swiperItem: {
    flexDirection: "column",
    marginTop: 15,
  },
});
/* import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    width: "100%",
    height: "100%",
    marginTop: 15,
  },
  title: {
    textAlign: "center",
    fontWeight: 500,
    fontSize: 20,
    marginBottom: 15,
  },
  swiperItem: {
    flexDirection: "column",
    marginTop: 15,
  },
});
 */
