import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    width: "100%",
    flexDirection: "column",
    gap: 10,
  },

  title: {
    fontSize: 16,
    fontWeight: 500,
    paddingBottom: 15
  },
  list: {
    width: 376,
    flexDirection: "column",
    gap: 10
  }
});
